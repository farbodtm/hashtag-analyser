extends layout

block content
  script
    | var page = 'cluster'; 
    | var hts = '#{hashtags}';
    | var cl = '#{clusters}';
    | var collection = '#{collection}';
    | var showHashtag = '#{show}';
